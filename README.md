# 🌸 Iris Flower Classification using Machine Learning

This project uses **Machine Learning** to classify iris flowers into three different species based on their sepal and petal measurements.

The three species are:

* 🌱 **Iris Setosa**
* 🌿 **Iris Versicolor**
* 🌺 **Iris Virginica**

## 📌 Project Overview

The **Iris Flower Classification** project is a beginner-friendly machine learning project that demonstrates the complete ML workflow, including data loading, data preprocessing, exploratory data analysis, model training, prediction, and evaluation.

The model learns patterns from flower measurements and predicts the species of a new iris flower.

## 🎯 Objective

The main objective of this project is to build a machine learning classification model that can accurately predict the species of an iris flower using:

* Sepal Length
* Sepal Width
* Petal Length
* Petal Width

## 📊 Dataset

The project uses the **Iris dataset**, which contains measurements of iris flowers belonging to three different species.

| Feature      | Description           |
| ------------ | --------------------- |
| Sepal Length | Length of the sepal   |
| Sepal Width  | Width of the sepal    |
| Petal Length | Length of the petal   |
| Petal Width  | Width of the petal    |
| Species      | Target flower species |

The dataset contains **150 samples**, with 50 samples from each species.

## 🛠️ Technologies Used

* **Python**
* **Pandas**
* **NumPy**
* **Matplotlib**
* **Seaborn**
* **Scikit-learn**
* Jupyter Notebook

## 🔄 Project Workflow

```text
Dataset
   ↓
Data Loading
   ↓
Data Exploration
   ↓
Data Preprocessing
   ↓
Train-Test Split
   ↓
Model Training
   ↓
Model Evaluation
   ↓
Flower Species Prediction
```

## 🤖 Machine Learning

The project can use a classification algorithm such as:

* Logistic Regression
* K-Nearest Neighbors (KNN)
* Decision Tree
* Random Forest
* Support Vector Machine (SVM)

The trained model takes the four flower measurements as input and predicts the corresponding iris species.

## 📈 Model Evaluation

The model can be evaluated using:

* Accuracy Score
* Confusion Matrix
* Classification Report
* Precision
* Recall
* F1-Score

## 📁 Project Structure

```text
Iris-Flower-Classification/
│
├── Iris_Flower_Classification.ipynb
├── README.md
├── dataset/
│   └── iris.csv
└── requirements.txt
```

## 🚀 How to Run

### 1. Clone the repository

```bash
git clone https://github.com/your-username/Iris-Flower-Classification.git
```

### 2. Navigate to the project

```bash
cd Iris-Flower-Classification
```

### 3. Install the required libraries

```bash
pip install -r requirements.txt
```

### 4. Open the Jupyter Notebook

```bash
jupyter notebook
```

Open:

```text
Iris_Flower_Classification.ipynb
```

and run the cells sequentially.

## 🔮 Example Prediction

For a new flower with:

```text
Sepal Length = 5.1
Sepal Width  = 3.5
Petal Length = 1.4
Petal Width  = 0.2
```

The model can predict:

```text
Predicted Species: Iris Setosa
```

## 📚 Learning Outcomes

Through this project, I learned:

* Basics of supervised machine learning
* Classification algorithms
* Data preprocessing
* Exploratory data analysis
* Model training and testing
* Model evaluation
* Making predictions using trained ML models

## 🔮 Future Improvements

* Build a web interface for real-time predictions
* Deploy the model using Flask or Streamlit
* Compare multiple ML algorithms
* Add interactive visualizations
* Improve the user experience with a prediction form

## 👩‍💻 Author

**Shreya Das**

B.Tech Computer Science & System Engineering
KIIT-DU

SCREENSHOT:-
## 📸 Project Screenshot

![Image]()
